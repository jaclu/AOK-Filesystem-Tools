#!/bin/sh

#
#  Triggered indirectly via ~/.profile
#  pointing ENV to this script
#
echo "--- ~/.env_init [$$] [$0] [$1]"

if [ -n "$ENV" ]; then
    echo "  probably triggered due to ENV [$ENV]"
fi
